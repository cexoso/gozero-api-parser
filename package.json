{
  "name": "go-zero/parser",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "publishConfig": {
    "directory": "dist",
    "linkDirectory": false
  },
  "scripts": {
    "test": "vitest",
    "build": "tsc --noEmit && vitest --run && ts-pkg-build build --format ESM",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "go-zero",
    "api parser"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "chevrotain": "^11.0.3"
  },
  "devDependencies": {
    "@cexoso/ts-pkg-build": "^0.0.19",
    "@changesets/cli": "^2.28.1",
    "@types/node": "^22.13.4",
    "ts-dedent": "^2.2.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.6"
  }
}
